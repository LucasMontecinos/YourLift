# ── Dependencias ────────────────────────────────────────
node_modules/
package-lock.json

# ── Firebase ─────────────────────────────────────────────
firebase-debug.log
.firebase/

# ── Datos privados (NUNCA subir — contienen RUTs y datos personales) ──
inscripciones.json
inscripciones.backup.json
entrenadores_centro_2026.json

# ── Backups de data.json ─────────────────────────────────
data.json.bak*
data_mal.json

# ── Scripts de mantenimiento (solo uso local) ────────────
*.mjs
relay.js
bridge.py
bridge.bat
transmision.py
bootstrap_lucas.html

# ── Videos y archivos grandes ───────────────────────────
*.mp4
videos_break/
videos_recortados/

# ── Logos en alta resolución (no necesarios en repo) ────
yourlift_logo_negro_*.png

# ── Archivos de Office / carpetas locales ───────────────
*.xlsx
Campeonatos/
data admin/
record actualizado*/

# ── Claude Code (configuración local) ───────────────────
.claude/

# ── OS ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
